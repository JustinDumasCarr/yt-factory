# Definition of Done

A task is done only when:
- Verification passes (`scripts/verify.sh`)
- `evidence.md` exists with results
- Tests pass (unless waived)
- Work is on a feature branch

Never commit directly to main.

